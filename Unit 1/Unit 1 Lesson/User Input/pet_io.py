# Prompt the user to enter their pet's type and store the response in pet_type
pet_type = input('Pet type: ')

# Prompt the user to enter their pet's name and store the response in pet_name
pet_name = input('Pet name: ')

# Output a message that includes the pet's name and type
print(pet_name, 'the', pet_type)